name: on-pr

on:
  pull_request:
    branches:
      - main
    types:
      - labeled
      - opened
      - reopened
      - synchronize
    paths:
      - '.github/workflows/on-pr.yaml'
      - '.gitops/**'
      - 'apis/**'
      - 'examples/**'
      - 'tests/**'
      - 'functions/**'
      - 'upbound.yaml'

permissions:
  packages: write
  contents: write
  issues: write
  pull-requests: write

jobs:
  validate:
    uses: unbounded-tech/workflows-crossplane/.github/workflows/validate.yaml@v2.3.0
    with:
      examples: |
        [
          { "example": "examples/networks/example-minimal.yaml" },
          { "example": "examples/networks/example-minimal.yaml", "observed_resources": "examples/observed-resources/example-minimal/steps/1" },
          { "example": "examples/networks/example-private-only.yaml" },
          { "example": "examples/networks/example-manual-cidr.yaml" },
          { "example": "examples/networks/example-dual-stack.yaml" },
          { "example": "examples/networks/example-enterprise.yaml" },
          { "example": "examples/networks/example-ipam-subnets.yaml" },
          { "example": "examples/networks/example-ipam-subnets.yaml", "observed_resources": "examples/observed-resources/example-ipam-subnets/steps/1" }
        ]
      api_path: apis/networks
      error_on_missing_schemas: true

  test:
    uses: unbounded-tech/workflows-crossplane/.github/workflows/test.yaml@v2.3.0

  e2e:
    uses: unbounded-tech/workflows-crossplane/.github/workflows/e2e.yaml@v2.3.0
    secrets:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
    with:
      aws: true
      timeout-minutes: 30
      debug-example-path: examples/networks/example-minimal.yaml
      debug-resource-types: |
        [
          "networks.aws.hops.ops.com.ai"
        ]

  publish:
    needs:
      - validate
      - test
    uses: unbounded-tech/workflows-crossplane/.github/workflows/publish.yaml@v2.3.0
    secrets: inherit
    with:
      tag: pr-${{ github.event.pull_request.number }}-${{ github.sha }}
